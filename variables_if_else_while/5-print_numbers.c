#include <stdio.h>

/**
 * main - entry point
 * Print every single digit base 10 number
 * Result: Always 0
**/
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
	printf("%d\n", i);
	}
	return (0);
}
